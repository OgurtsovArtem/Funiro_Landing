const CHANGE_FORM_SETTINGS = {
  changeFormBtn: [...document.querySelectorAll("._change-form-btn")],
  formList: [...document.querySelectorAll("._change-form")],
  dataName: "data-change-form", // Передаем дата-атрибут
};

export default CHANGE_FORM_SETTINGS;
